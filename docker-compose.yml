name: zublo
services:
  app:
    build: .  # ⬅️ THIS BUILDS YOUR LOCAL CODE. Change from 'image:' to 'build:'
    container_name: zublo
    restart: always
    ports:
      - "9597:9597"
    environment:
      PB_ENCRYPTION_KEY: ${PB_ENCRYPTION_KEY}
    volumes:
      - zublo-volume:/pb/pb_data
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9597/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

volumes:
  zublo-volume:
